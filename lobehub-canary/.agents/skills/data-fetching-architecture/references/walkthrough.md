# Walkthrough: Adding a New Feature End-to-End
Canonical 6-step recipe: Service methods -> Reducer -> Store slice -> Wire into store -> Selectors -> Component.
Shows the Map-variant pattern where lists are keyed by parent ID.